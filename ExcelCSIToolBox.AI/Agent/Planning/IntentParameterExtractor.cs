namespace ExcelCSIToolBox.AI.Agent
{
    public sealed class IntentParameterExtractor
    {
        // Domain-specific extraction is still kept in CsiIntentPlannerService while the
        // registry migration settles; this type marks the planning seam for the next split.
    }
}
