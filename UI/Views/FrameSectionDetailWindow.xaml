<Window x:Class="ExcelCSIToolBoxAddIn.UI.Views.FrameSectionDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Section Property" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="12">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Section Data" Grid.Row="0" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Section Name" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Text="{Binding SectionName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                
                <TextBlock Grid.Column="2" Text="Display Color" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Border Grid.Column="3" Background="Yellow" BorderBrush="Black" BorderThickness="1"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <GroupBox Header="Dimensions" Grid.Column="0" Margin="0,0,5,0" Padding="10">
                <ItemsControl ItemsSource="{Binding Dimensions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Key}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                                <TextBox Grid.Column="1" Text="{Binding ValueText, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Right" Width="70"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>
            
            <GroupBox Header="Section Shape" Grid.Column="1" Margin="5,0,0,0" Padding="10">
                <Border BorderBrush="Gray" BorderThickness="1" Background="#F9F9F9">
                    <Viewbox Margin="10">
                        <Canvas x:Name="PreviewCanvas" Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <!-- Shape drawn dynamically in code-behind -->
                        </Canvas>
                    </Viewbox>
                </Border>
            </GroupBox>
        </Grid>
        
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <GroupBox Header="Material" Grid.Column="0" Margin="0,0,5,0" Padding="10">
                <TextBox Text="{Binding MaterialName, UpdateSourceTrigger=PropertyChanged}"/>
            </GroupBox>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="OK" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
                <Button Content="Cancel" Width="80" IsCancel="True" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
