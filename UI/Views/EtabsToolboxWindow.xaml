<Window x:Class="ExcelCSIToolBoxAddIn.UI.Views.EtabsToolboxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding ProductTitle, FallbackValue=ETABS Toolbox}"
        Width="800"
        Height="800"
        MinWidth="800"
        MinHeight="800"
        MaxWidth="800"
        MaxHeight="800"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        FontSize="11">

    <Window.Resources>
        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="MinHeight" Value="36"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1F6AA5"/>
            <Setter Property="BorderBrush" Value="#1B5C90"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2A7BBE"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#236B9F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#184F7C"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#14456B"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#B7C4D0"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#A8B5C1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top area -->
        <Border Grid.Row="0"
                Padding="10"
                Margin="0,0,0,10"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                CornerRadius="4"
                Background="#FAFAFA">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding ProductTitle, FallbackValue=ETABS Toolbox}"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Width="32"
                            Height="32"
                            Padding="0"
                            Content="?"
                            Click="HelpButton_Click"/>
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Text="Model Name:"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="Black"
                               Margin="0,0,8,0"/>

                    <TextBox Grid.Row="0"
                             Grid.Column="1"
                             Text="{Binding ModelDisplayText, Mode=OneWay}"
                             IsReadOnly="True"
                             BorderThickness="1"
                             Background="White"
                             VerticalContentAlignment="Center"
                             FontWeight="Bold"
                             Height="26"/>

                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Text="Current Model Unit:"
                               VerticalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="Black"
                               Margin="0,0,8,0"/>

                    <TextBox Grid.Row="2"
                             Grid.Column="1"
                             Text="{Binding CurrentModelUnitText, Mode=OneWay}"
                             IsReadOnly="True"
                             BorderThickness="1"
                             Background="White"
                             VerticalContentAlignment="Center"
                             FontWeight="Bold"
                             Height="26"/>
                </Grid>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left">
                    <Button Content="Attach"
                            Width="70"
                            Margin="0,0,8,0"
                            Command="{Binding AttachToRunningCsiCommand}"/>

                    <Button Content="Close"
                            Width="70"
                            Command="{Binding CloseCurrentInstanceCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main content frame -->
        <Border Grid.Row="1"
                Padding="8"
                BorderBrush="#CFCFCF"
                BorderThickness="1"
                CornerRadius="4"
                Background="#FFFFFF">

            <TabControl>
                <TabItem Header="Section Property">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>
                        
                        <GroupBox Grid.Column="0" Header="Existing Sections" FontWeight="Bold" Padding="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ListView x:Name="FrameSectionList" Grid.Row="0" 
                                          ItemsSource="{Binding FrameSections}"
                                          SelectedItem="{Binding SelectedFrameSection}"
                                          BorderBrush="#CCCCCC" BorderThickness="1"
                                          FontWeight="Normal" Foreground="Black">
                                    <ListView.InputBindings>
                                        <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditFrameSectionCommand}" CommandParameter="{Binding ElementName=FrameSectionList, Path=SelectedItem}" />
                                    </ListView.InputBindings>
                                    <ListView.View>
                                        <GridView>
                                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="190"/>
                                            <GridViewColumn Header="Type" DisplayMemberBinding="{Binding ShapeType}" Width="90"/>
                                        </GridView>
                                    </ListView.View>
                                </ListView>
                                <Button Grid.Row="1" Margin="0,8,0,0" Content="Refresh List" Command="{Binding GetFrameSectionsCommand}"/>
                            </Grid>
                        </GroupBox>
                        
                        <GroupBox Grid.Column="2" Header="Section Preview &amp; Creation" FontWeight="Bold" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <Border Grid.Row="0" BorderBrush="#DDDDDD" BorderThickness="1" Background="#F9F9F9" Margin="0,0,0,10" MinHeight="160">
                                    <Grid Margin="10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Viewbox Grid.Row="0" Grid.Column="1" Stretch="Uniform">
                                            <Canvas x:Name="EtabsSectionPreviewCanvas" Width="100" Height="100"/>
                                        </Viewbox>
                                        <Border Grid.Row="0"
                                                Grid.Column="0"
                                                Margin="0,0,10,0"
                                                Padding="8"
                                                BorderBrush="#E1E1E1"
                                                BorderThickness="1"
                                                Background="White">
                                            <StackPanel>
                                                <TextBlock Text="Dimensions"
                                                           FontWeight="SemiBold"
                                                           Foreground="#333333"
                                                           Margin="0,0,0,6"/>
                                                <ItemsControl ItemsSource="{Binding SectionDimensionAnnotations}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Margin="0,0,0,6">
                                                                <TextBlock Text="{Binding DisplayText}"
                                                                           Foreground="#D02020"
                                                                           FontSize="10"
                                                                           FontWeight="SemiBold"/>
                                                                <TextBlock Text="{Binding DescriptionText}"
                                                                           Foreground="#777777"
                                                                           FontSize="9"
                                                                           FontStyle="Italic"
                                                                           TextWrapping="Wrap"
                                                                           Margin="0,1,0,0"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,6,0,0">
                                            <TextBlock x:Name="EtabsSectionNameLabel" Text="—" HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="#333333" FontSize="11"/>
                                            <TextBlock x:Name="EtabsSectionTypeLabel" Text="" HorizontalAlignment="Center" Foreground="#888888" FontSize="10"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                
                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="Create New Section From Excel" Margin="0,0,0,8"/>
                                    <WrapPanel>
                                        <Button Content="Ishape" Width="70" Margin="0,0,8,8" Command="{Binding CreateIshapeSectionCommand}" />
                                        <Button Content="Channel" Width="70" Margin="0,0,8,8" Command="{Binding CreateChannelSectionCommand}" />
                                        <Button Content="Angle" Width="70" Margin="0,0,8,8" Command="{Binding CreateAngleSectionCommand}" />
                                        <Button Content="Tube" Width="70" Margin="0,0,8,8" Command="{Binding CreateTubeSectionCommand}" />
                                        <Button Content="Pipe" Width="70" Margin="0,0,8,8" Command="{Binding CreatePipeSectionCommand}" />
                                        <Button Content="Rectangle" Width="70" Margin="0,0,8,8" Command="{Binding CreateConcreteRectangleSectionCommand}" />
                                        <Button Content="Circle" Width="70" Margin="0,0,8,8" Command="{Binding CreateConcreteCircleSectionCommand}" />
                                    </WrapPanel>
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                    </Grid>
                </TabItem>

                <TabItem Header="Point">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="True">
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="1"
                                       Text="Select Point in Model from Excel Range"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="0"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="By UniqueName"
                                    Command="{Binding SelectPointsByUniqueNameCommand}"/>

                            <TextBlock Grid.Row="2"
                                       Grid.Column="1"
                                       Text="Add Point From Range Excel"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="2"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="By Cartesian"
                                    HorizontalAlignment="Stretch"
                                    Command="{Binding AddPointByCartesianCommand}"/>

                            <TextBlock Grid.Row="4"
                                       Grid.Column="1"
                                       Text="Extract Selected Point Information"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="4"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Run"
                                    HorizontalAlignment="Stretch"
                                    Command="{Binding GetSelectedPointsCommand}"/>

                            <TextBlock Grid.Row="6"
                                       Grid.Column="1"
                                       Text="Grouping"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="6"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Get"
                                    HorizontalAlignment="Stretch"
                                    Command="{Binding GetPointGroupAssignmentCommand}"/>

                            <Button Grid.Row="6"
                                    Grid.Column="4"
                                    Margin="6,0,6,0"
                                    Content="Set"
                                    HorizontalAlignment="Stretch"
                                    Command="{Binding SetPointGroupAssignmentCommand}"/>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Frame">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="True">
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="2.2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="1"
                                       Text="Get Current Selection Frame UniqueNames"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="0"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Run"
                                    Command="{Binding GetSelectedFramesCommand}"/>

                            <TextBlock Grid.Row="2"
                                       Grid.Column="1"
                                       Text="Select Frame in Model from Excel Range"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="2"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="By UniqueName"
                                    Command="{Binding SelectFramesByUniqueNameCommand}"/>

                            <TextBlock Grid.Row="4"
                                       Grid.Column="1"
                                       Text="Add Frame From Range Excel"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="4"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="By Coor"
                                    Command="{Binding AddFramesByCoordinatesCommand}"/>

                            <Button Grid.Row="4"
                                    Grid.Column="4"
                                    Margin="6,0,6,0"
                                    Content="By Point"
                                    Command="{Binding AddFramesByPointNamesCommand}"/>

                            <TextBlock Grid.Row="6"
                                       Grid.Column="1"
                                       Text="Extract Selected Frame Information"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="6"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Run"
                                    Command="{Binding GetSelectedFramesCommand}"/>

                            <TextBlock Grid.Row="8"
                                       Grid.Column="1"
                                       Text="Change Name"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="8"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Run"
                                    Command="{Binding RenameFramesCommand}"/>

                            <TextBlock Grid.Row="10"
                                       Grid.Column="1"
                                       Text="Frame Section Property"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="10"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Get"
                                    Command="{Binding GetFrameSectionPropertyCommand}"/>

                            <Button Grid.Row="10"
                                    Grid.Column="4"
                                    Margin="6,0,6,0"
                                    Content="Set"
                                    Command="{Binding SetFrameSectionPropertyCommand}"/>

                            <TextBlock Grid.Row="12"
                                       Grid.Column="1"
                                       Text="Frame Group Assignment"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="12"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Get"
                                    Command="{Binding GetFrameGroupAssignmentCommand}"/>

                            <Button Grid.Row="12"
                                    Grid.Column="4"
                                    Margin="6,0,6,0"
                                    Content="Set"
                                    Command="{Binding SetFrameGroupAssignmentCommand}"/>

                            <TextBlock Grid.Row="14"
                                       Grid.Column="1"
                                       Text="Frame Modifier"
                                       VerticalAlignment="Center"
                                       Foreground="Black"
                                       FontWeight="Bold"
                                       TextWrapping="Wrap"
                                       Margin="0,0,8,0"/>

                            <Button Grid.Row="14"
                                    Grid.Column="2"
                                    Margin="6,0,6,0"
                                    Content="Get"
                                    Command="{Binding GetFrameModifierCommand}"/>

                            <Button Grid.Row="14"
                                    Grid.Column="4"
                                    Margin="6,0,6,0"
                                    Content="Set"
                                    Command="{Binding SetFrameModifierCommand}"/>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Shell">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="True">
                        <StackPanel Margin="12">
                            <GroupBox Header="Shell Area" FontWeight="Bold" Margin="0,0,0,10" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2.2*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="Create Shell Area From Selected Frames"
                                               VerticalAlignment="Center"
                                               Foreground="Black"
                                               FontWeight="Bold"
                                               TextWrapping="Wrap"
                                               Margin="0,0,8,0"/>

                                    <Button Grid.Column="2"
                                            Content="Create Shell"
                                            HorizontalAlignment="Stretch"
                                            Command="{Binding CreateShellAreasFromSelectedFramesCommand}"/>
                                </Grid>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="LoadPattern">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="True">
                        <StackPanel Margin="12">
                            <GroupBox Header="Load Patterns" FontWeight="Bold" Margin="0,0,0,10" Padding="10">
                                <Grid Height="180">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="150"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Existing Patterns in Model" Foreground="Black" Margin="0,0,0,4"/>
                                    <ListView x:Name="LoadPatternList" Grid.Row="1" Grid.Column="0" 
                                              ItemsSource="{Binding LoadPatterns}"
                                              SelectionMode="Extended"
                                              BorderBrush="#CCCCCC" BorderThickness="1"
                                              FontWeight="Normal" Foreground="Black">
                                        <ListView.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Delete" Command="{Binding PlacementTarget.DataContext.DeleteSelectedLoadPatternsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </ContextMenu>
                                        </ListView.ContextMenu>
                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Header="Load Pattern Name" DisplayMemberBinding="{Binding Name}" Width="250"/>
                                                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="150"/>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                    
                                    <StackPanel Grid.Row="1" Grid.Column="2" VerticalAlignment="Top">
                                        <Button Content="Refresh List" HorizontalAlignment="Stretch" Command="{Binding GetLoadPatternsCommand}" Margin="0,0,0,8"/>
                                        <Button Content="Add From Excel" HorizontalAlignment="Stretch" Command="{Binding AddLoadPatternFromExcelCommand}" Margin="0,0,0,8"/>
                                    </StackPanel>
                                </Grid>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="LoadCombination">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="True">
                        <StackPanel Margin="12">

                            <GroupBox Header="Load Combination" FontWeight="Bold" Margin="0,0,0,10" Padding="10">
                                <Grid Height="180">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="150"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Existing Combinations in Model" Foreground="Black" Margin="0,0,0,4"/>
                                    <ListView x:Name="LoadCombinationList" Grid.Row="1" Grid.Column="0" 
                                              ItemsSource="{Binding LoadCombinations}"
                                              SelectionMode="Extended"
                                              BorderBrush="#CCCCCC" BorderThickness="1"
                                              FontWeight="Normal" Foreground="Black">
                                        <ListView.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="View" Command="{Binding PlacementTarget.DataContext.ViewLoadCombinationCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Delete" Command="{Binding PlacementTarget.DataContext.DeleteSelectedLoadCombinationsCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </ContextMenu>
                                        </ListView.ContextMenu>
                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Header="Combination Name" DisplayMemberBinding="{Binding Name}" Width="250"/>
                                                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="150"/>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                    
                                    <StackPanel Grid.Row="1" Grid.Column="2" VerticalAlignment="Top">
                                        <Button Content="Refresh List" HorizontalAlignment="Stretch" Command="{Binding GetLoadCombinationsCommand}" Margin="0,0,0,8"/>
                                        <Button Content="Add From Excel" HorizontalAlignment="Stretch" Command="{Binding AddLoadCombinationFromExcelCommand}" Margin="0,0,0,8"/>
                                    </StackPanel>
                                </Grid>
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</Window>
