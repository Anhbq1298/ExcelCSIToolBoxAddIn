# ViewModels
MVVM ViewModels with commands and bindable properties.
- Each ViewModel implements INotifyPropertyChanged.
- User actions are handled via Command objects.
- Services are injected through the constructor.
- No direct CSI or Excel interop calls inside ViewModels.
